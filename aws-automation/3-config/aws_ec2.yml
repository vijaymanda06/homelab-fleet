plugin: aws_ec2
regions:
  - us-east-1
filters:
  tag:Project: ZeroTrust
strict: False
keyed_groups:
  - key: tags.Name
    prefix: tag
compose:
  ansible_connection: "'aws_ssm'"
  ansible_aws_ssm_region: "'us-east-1'"
